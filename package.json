{
  "name": "@nestjs/cqrs",
  "version": "4.1.0",
  "description": "A lightweight CQRS module for Nest framework (node.js)",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "precommit": "lint-staged",
    "prettier": "prettier */**/*.ts --write && git status"
  },
  "dependencies": {
    "@nestjs/common": "^4.*",
    "reflect-metadata": "0.1.12",
    "rxjs": "^5.5.8",
    "typescript": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^7.0.41",
    "husky": "^0.14.3",
    "lint-staged": "^7.0.0",
    "prettier": "^1.11.1"
  },
  "lint-staged": {
    "*.{js,ts,json,css,md}": ["prettier --write", "git add"]
  }
}
